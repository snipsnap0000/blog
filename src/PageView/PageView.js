import * as React from 'react';
import ContentContainer from '../ContentContainer/ContentContainer';

const s = require('./PageView.css');

const PageView = () => (
  <ContentContainer className={s.pageView}>
    content
  </ContentContainer>
);

export default PageView;
