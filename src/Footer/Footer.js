import * as React from 'react';
import ContentContainer from '../ContentContainer/ContentContainer';

const s = require('./Footer.css');

const Footer = () => (
  <ContentContainer className={s.footer}>
    asd
  </ContentContainer>
);

export default Footer;
